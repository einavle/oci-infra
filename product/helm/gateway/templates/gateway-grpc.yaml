apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: {{ .Release.Name }}-gateway-grpc
spec:
  selector:
    istio: ingress-grpc
  servers:
  - port:
      number: 443
      name: https
      protocol: HTTPS
    hosts:
      {{- range .Values.gateway.hosts }}
      - {{ . | quote }}
      {{- end }}
    tls:
      mode: SIMPLE
      credentialName: israel-takeaway-tls
  
