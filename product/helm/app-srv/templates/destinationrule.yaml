apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ .Release.Name }}-dr
spec:
  host: {{ .Release.Name }}
  subsets:
    - name: canary
      labels:
        version: canary
    - name: stable
      labels:
        version: stable
